node_modules/
.pnpm-store/

# Generated by the devcontainers publish action from devcontainer-feature.json.
# Formatting it here would fight that generator on every publish.
features/src/*/README.md

pnpm-lock.yaml

# Generated and committed by semantic-release (@semantic-release/changelog) through
# @semantic-release/git, so it never passes through lint-staged. The copy in git is
# therefore unformatted, and any human commit touching it would make lint-staged reflow
# the whole changelog — hundreds of lines of churn to edit two.
HISTORY.md
